<%= form_for([@post, @post.comments.build]) do |f| %>
	<p>
		<%= f.label :email %>
		<%= f.text_field :email %>
	</p>

	<p>
		<%= f.label :text %>
		<%= f.text_area :text %>
	<p>

	<%= f.submit %>
<% end %>