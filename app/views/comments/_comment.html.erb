<blockquote>
	<p><%= comment.email %><p>
	<p><%= comment.text %><p>
	<footer><%= link_to 'Destroy Comment', [comment.post, comment], method: :delete, data: { confirm: 'Are you sure?' } if @is_owner %></footer>
</blockquote>